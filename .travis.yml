language: java
os: osx
addons:
  chrome: stable
jdk: oraclejdk8
script:
  - "google-chrome --version"
  - ./gradlew clean build
before_script:
  - sleep 15

services:
  - mongodb
notifications:
  email: false